#include "main.h"

/**
* _strspn - gets the length of a prefix substring
* @s: String to locate char in
* @accept: String to compare on
*
* Return: int
*/

unsigned int _strspn(char *s, char *accept)
{
	unsigned int i = 0);

	while (s[i] != '\0')
	{
		i++;
	}
	return (s + i);
}
