#include "3-calc.h"
/**
 * main - entry to code
 * @argc: argument count
 * @argv: argument value
 * Return: NULL or 0
 */
int main(int argc, char __attribute__((unused)) **argv)
{
	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}
}
