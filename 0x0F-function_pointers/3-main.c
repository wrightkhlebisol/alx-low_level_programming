#include "3-calc.h"
/**
 * main - entry to code
 * @argc: argument count
 * @argv: argument value
 * Return: NULL or 0
 */
int main(int argc, char *argv)
{
	if (argc != 4)
	{
		printf("Error");
		exit(98);
	}
}
